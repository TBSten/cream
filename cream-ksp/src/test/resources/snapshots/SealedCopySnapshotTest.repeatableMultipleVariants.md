## Generated

````kt
// file: SealedCopy__MyState.kt
package snap.sealedCopy.repeatable

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [MyState])
 * 
 * Type-preserving MyState.withUpdated generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.withUpdated()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.withUpdated(property = value)
 * ```
 * 
 * 
 * @see MyState
 * @see MyState.Empty
 * @see MyState.Loading
 */
public fun snap.sealedCopy.repeatable.MyState.withUpdated(
    name: String = this.name,
): snap.sealedCopy.repeatable.MyState = when (this) {
    is snap.sealedCopy.repeatable.MyState.Empty -> this
    is snap.sealedCopy.repeatable.MyState.Loading -> this.copy(name = name)
}

/**
 * (Auto generate by @[SealedCopy] annotation of [MyState])
 * 
 * Type-preserving MyState.withUpdatedOrNull generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState? = state.withUpdatedOrNull()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState? = state.withUpdatedOrNull(property = value)
 * ```
 * 
 * 
 * @see MyState
 * @see MyState.Empty
 * @see MyState.Loading
 */
public fun snap.sealedCopy.repeatable.MyState.withUpdatedOrNull(
    name: String = this.name,
): snap.sealedCopy.repeatable.MyState? = when (this) {
    is snap.sealedCopy.repeatable.MyState.Empty -> null
    is snap.sealedCopy.repeatable.MyState.Loading -> this.copy(name = name)
}
````

## Input

```kt
package snap.sealedCopy.repeatable

import me.tbsten.cream.NonCopyableStrategy
import me.tbsten.cream.SealedCopy

@SealedCopy(funName = "withUpdated", nonCopyableStrategy = NonCopyableStrategy.RETURN_AS_IS)
@SealedCopy(funName = "withUpdatedOrNull", nonCopyableStrategy = NonCopyableStrategy.RETURN_NULL)
sealed interface MyState {
    val name: String

    data class Loading(override val name: String) : MyState
    data object Empty : MyState { override val name: String = "" }
}
```
