## Generated

````kt
// file: SealedCopy__Box.kt
package snap.sealedCopy.extra

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [Box])
 * 
 * Type-preserving Box.copy generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: Box = /* one of the subtypes */
 * val updated: Box = state.copy()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: Box = /* one of the subtypes */
 * val updated: Box = state.copy(property = value)
 * ```
 * 
 * 
 * @see Box
 * @see Box.Plain
 * @see Box.Tagged
 */
public fun <T : Any?> snap.sealedCopy.extra.Box<T>.copy(
    value: T = this.value,
): snap.sealedCopy.extra.Box<T> = when (this) {
    is snap.sealedCopy.extra.Box.Plain -> this.copy(value = value)
    is snap.sealedCopy.extra.Box.Tagged<T, *> -> this.copy(value = value)
}
````

## Input

```kt
package snap.sealedCopy.extra

import me.tbsten.cream.SealedCopy

@SealedCopy
sealed interface Box<T> {
    val value: T

    data class Tagged<T, M>(override val value: T, val meta: M) : Box<T>
    data class Plain<T>(override val value: T) : Box<T>
}
```
