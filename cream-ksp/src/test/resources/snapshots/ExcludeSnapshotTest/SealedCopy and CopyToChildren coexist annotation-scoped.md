## Generated

````kt
// file: CopyToChildren__DualState.kt
package snap.exclude

import me.tbsten.cream.*

/**
 * (Auto generate by @[CopyToChildren] annotation of [DualState])
 * 
 * DualState -> DualState.A copy function.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val source = DualState(...)
 * val target = source.copyToDualStateA(score = score)
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val source = DualState(...)
 * val target = source.copyToDualStateA(score = score, property = value)
 * ```
 * 
 * 
 * @see DualState
 * @see DualState.A
 */
public fun  snap.exclude.DualState.copyToDualStateA(
    name: String = this.name,
    count: Int = this.count,
    score: Int,
) : snap.exclude.DualState.A = snap.exclude.DualState.A(
    name = name,
    count = count,
    score = score,
)

// ----- next file -----

// file: SealedCopy__DualState.kt
package snap.exclude

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [DualState])
 * 
 * Type-preserving DualState.copy generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: DualState = /* one of the subtypes */
 * val updated: DualState = state.copy(count = count)
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: DualState = /* one of the subtypes */
 * val updated: DualState = state.copy(count = count, property = value)
 * ```
 * 
 * 
 * @see DualState
 * @see DualState.A
 */
public fun snap.exclude.DualState.copy(
    name: String = this.name,
    count: Int,
    score: Int = this.score,
): snap.exclude.DualState = when (this) {
    is snap.exclude.DualState.A -> this.copy(name = name, count = count, score = score)
}
````

## Input

```kt
package snap.exclude

import me.tbsten.cream.SealedCopy
import me.tbsten.cream.CopyToChildren

@SealedCopy
@CopyToChildren
sealed interface DualState {
    val name: String
    @SealedCopy.Exclude val count: Int
    @CopyToChildren.Exclude val score: Int
    data class A(
        override val name: String,
        override val count: Int,
        override val score: Int,
    ) : DualState
}
```
