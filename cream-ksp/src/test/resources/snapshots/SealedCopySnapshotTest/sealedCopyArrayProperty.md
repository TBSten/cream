## Generated

````kt
// file: SealedCopy__MyState.kt
package snap.sealedCopy.arrayProp

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [MyState])
 * 
 * Type-preserving MyState.copy generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.copy()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.copy(property = value)
 * ```
 * 
 * 
 * @see MyState
 * @see MyState.Loading
 * @see MyState.Success
 */
public fun snap.sealedCopy.arrayProp.MyState.copy(
    tags: Array<String> = this.tags,
): snap.sealedCopy.arrayProp.MyState = when (this) {
    is snap.sealedCopy.arrayProp.MyState.Loading -> this.copy(tags = tags)
    is snap.sealedCopy.arrayProp.MyState.Success -> this.copy(tags = tags)
}
````

## Input

```kt
package snap.sealedCopy.arrayProp

import me.tbsten.cream.SealedCopy

@SealedCopy
sealed interface MyState {
    val tags: Array<String>

    data class Loading(override val tags: Array<String>) : MyState
    data class Success(override val tags: Array<String>, val data: String) : MyState
}
```
