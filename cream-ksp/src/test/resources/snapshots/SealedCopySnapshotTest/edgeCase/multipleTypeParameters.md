## Generated

````kt
// file: SealedCopy__Entry.kt
package snap.sealedCopy.multiGeneric

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [Entry])
 * 
 * Type-preserving Entry.copy generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: Entry = /* one of the subtypes */
 * val updated: Entry = state.copy()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: Entry = /* one of the subtypes */
 * val updated: Entry = state.copy(property = value)
 * ```
 * 
 * 
 * @see Entry
 * @see Entry.Impl
 */
public fun <K : Any?, V : Any?> snap.sealedCopy.multiGeneric.Entry<K, V>.copy(
    key: K = this.key,
    value: V = this.value,
): snap.sealedCopy.multiGeneric.Entry<K, V> = when (this) {
    is snap.sealedCopy.multiGeneric.Entry.Impl -> this.copy(key = key, value = value)
}
````

## Input

```kt
package snap.sealedCopy.multiGeneric

import me.tbsten.cream.SealedCopy

@SealedCopy
sealed interface Entry<K, V> {
    val key: K
    val value: V

    data class Impl<K, V>(override val key: K, override val value: V, val tag: String) : Entry<K, V>
}
```
