## Generated

````kt
// file: SealedCopy__MyState.kt
package snap.sealedCopy.funName

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [MyState])
 * 
 * Type-preserving MyState.updated generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.updated()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: MyState = /* one of the subtypes */
 * val updated: MyState = state.updated(property = value)
 * ```
 * 
 * 
 * @see MyState
 * @see MyState.Loading
 * @see MyState.Success
 */
public fun snap.sealedCopy.funName.MyState.updated(
    name: String = this.name,
): snap.sealedCopy.funName.MyState = when (this) {
    is snap.sealedCopy.funName.MyState.Loading -> this.copy(name = name)
    is snap.sealedCopy.funName.MyState.Success -> this.copy(name = name)
}
````

## Input

```kt
package snap.sealedCopy.funName

import me.tbsten.cream.SealedCopy

@SealedCopy(funName = "updated")
sealed interface MyState {
    val name: String

    data class Loading(override val name: String) : MyState
    data class Success(override val name: String, val data: String) : MyState
}
```
