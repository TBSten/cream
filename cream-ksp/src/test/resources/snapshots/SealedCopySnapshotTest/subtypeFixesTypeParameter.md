## Generated

````kt
// file: SealedCopy__Box.kt
package snap.sealedCopy.fixed

import me.tbsten.cream.*

/**
 * (Auto generate by @[SealedCopy] annotation of [Box])
 * 
 * Type-preserving Box.copy generated by @SealedCopy.
 * 
 * Updates shared abstract properties and dispatches to each subtype's copy.
 * 
 * # Example: Basic
 * 
 * ```kt
 * val state: Box = /* one of the subtypes */
 * val updated: Box = state.copy()
 * ```
 * 
 * # Example: Override property values
 * 
 * ```kt
 * val state: Box = /* one of the subtypes */
 * val updated: Box = state.copy(property = value)
 * ```
 * 
 * 
 * @see Box
 * @see Box.Filled
 * @see Box.IntBox
 */
public fun <T : Any?> snap.sealedCopy.fixed.Box<T>.copy(
    value: T = this.value,
): snap.sealedCopy.fixed.Box<T> = when (this) {
    is snap.sealedCopy.fixed.Box.Filled -> this.copy(value = value)
    is snap.sealedCopy.fixed.Box.IntBox -> this.copy(value = value)
}
````

## Input

```kt
package snap.sealedCopy.fixed

import me.tbsten.cream.SealedCopy

@SealedCopy
sealed interface Box<T> {
    val value: T

    data class Filled<T>(override val value: T) : Box<T>
    data class IntBox(override val value: Int) : Box<Int>
}
```
